# Snowflake · Airflow 3: history

Everything the matrix row for **Snowflake · Airflow 3** once carried, verbatim and in the
order it was written, newest first. The row in [01-plan.md](../01-plan.md)
now holds only the current state and points here; nothing was cut in the
move. Each block below was the whole of the row at the time, and each
`Previously` block is what the block above it replaced. They are not dated
because the originals were not; the witness ids inside them are.

## Current entry

**BUILT, AND IT EARNED ITS KEEP ON THE FIRST RUN.** Both repositories were LICENSE-and-README reservations this morning ([leaf#1](https://github.com/calvinchengx/contoso-data-product-snowflake-airflow3/pull/1), [platform#2](https://github.com/calvinchengx/snowflake-platform-airflow3/pull/2)). **28/28 task instances succeeded** on `verify__20260820T132216Z` — provision, four MAPPED vendor ingests, bronze, an 8-model silver group with its whole-suite test, a 9-model gold group with its whole-suite test, and publish. `silver PASS=13` with `silver_orders_never_holds_a_non_positive_quantity` named in the log; `gold PASS=52` with **all five ODCS contracts named**; `129,341,157.6700 / 2,800,504.4000 / 474,044` and **every gold row count identical to the Tasks cell** — `dim_party` 118,000, `fct_sales` 474,044, `fct_orders` 247,500, and `fct_revenue_summary` **84**, which is G42's calendar effect reproducing rather than an engine difference. **DoD 3 IS MET HERE AND NOT IN THE TASKS CELL**: this cell is named for Airflow 3 and ran through Airflow 3. **DoD 4 too** — `compare_products` now exits 0 **five ways** (a `--snowflake-airflow` flag was added for it). **What it ran against, named rather than implied:** `ghcr.io/calvinchengx/snowflake-emulator:0.1.6` with the binary inside the container answering `v0.1.6`, core **v0.3.1**, cold volumes, ERP watermark **93,571** (30,000 I / 62,371 U / 1,200 D) against 28,800 rows — exactly one seed. **AND THE POINT OF BUILDING IT, which no other cell could have shown: the first run found **G45**** — the emulator refused concurrent statements, three of eight silver models failing on a duckdb file lock while five succeeded. Every prior consumer was sequential. Fixed, released as **v0.1.6**, and both pins moved before this witness. Two smaller findings, both from running rather than reading: the named volume that removes the stage's two-PATH problem introduces a two-USER one (all four `land` tasks died on `Permission denied: '/stages/…'` until a root one-shot chmods it), and `make down` could not run from a clean checkout because it needs the gitignored vendor fragment. Carries **G43** (two pins in two repositories) and, from its siblings, G29's shape guarded rather than met: both dbt groups use `AFTER_ALL` and a test asserts the shape, checked against `AFTER_EACH` **UNATTENDED VERIFICATION NOW EXISTS AND CANNOT PASS YET (G47).** This cell had no acceptance workflow at all until 2026-08-21: the ✅ above rests on a witness somebody ran by hand, and nothing renewed it. It has one now, plus a nightly. It gets past `make up` and stops at `verify`, because the DAG ships paused (G40's fix). **Verifies itself unattended and green as of 2026-08-22** (G47, G48 and G49 all closed). **The green now asserts the NUMBERS too (G50 closed):** the acceptance run holds this cell's `product_snapshot.json` to `contoso_product.EXPECTED` and printed `129341157.6700 / 2800504.4000 / 474044` unattended. The pipeline is not implicated; the automation is.
