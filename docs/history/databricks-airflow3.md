# Databricks · Airflow 3: history

Everything the matrix row for **Databricks · Airflow 3** once carried, verbatim and in the
order it was written, newest first. The row in [01-plan.md](../01-plan.md)
now holds only the current state and points here; nothing was cut in the
move. Each block below was the whole of the row at the time, and each
`Previously` block is what the block above it replaced. They are not dated
because the originals were not; the witness ids inside them are.

## Current entry

**Re-witnessed on NEWER PINS through `make verify`, and the figures did not move.** `verify__20260820T090629Z`, exit 0: **21/21 tasks**, gold `PASS=52 WARN=0 ERROR=0 SKIP=0 TOTAL=52`, `129,341,157.6700 / 2,800,504.4000 / 474,044`, every gold row count unchanged, `compare_products` exit 0 five ways. **Cosmos registered ZERO assets** across the whole run (`Dynamically creating AssetModel`: 0 occurrences) — the mechanism behind **G37**, which this cell carried unknowingly until `emit_datasets=False` closed it. **Two variables moved, which makes this worth more than a repeat:** the pins had been bumped (`405ddea`) to `databricks-emulator` **0.2.7** and Sail / spark-agent **0.32.0**, from 0.2.5 / 0.30.0, so the run exercises the G37 fix *and* a newer emulator, and the family's figures held to the last digit across both. Unity Catalog **v0.5.0** unchanged. **This is also the first witness in the family produced by one command that could have gone red** (G35) rather than by a person reading run state. Preconditions read: core **0.3.0** asserted from inside the worker, cold start with the delta directory, emulator `data/` and the PAT all deleted, ERP watermark **93,571** — exactly one seed. *It also found* **G40**: the scheduled run that auto-fires on stack start failed 16s in with `_UNKNOWN_PARTITION`, because Airflow began scheduling before Debezium had created the CDC topic.

## Previously (1)

**The medallion through Airflow 3 against Databricks, producing the family's numbers to the last digit — re-witnessed on core v0.3.0, from EMPTY VOLUMES, under the project name the repository ships.** The DAG renders **18 tasks** and ran **21/21 instances** (`land` is mapped over four vendors) on `scheduled__2026-08-19T00:00:00+00:00` — Airflow's own scheduler, not a hand trigger: provision, seed_secrets, four vendor ingests (three HTTP + one CDC), bronze, silver, register, **9 gold model tasks** and **one whole-suite contract task** reporting `PASS=52 WARN=0 ERROR=0 SKIP=0 TOTAL=52` with all five ODCS contracts named in its log, then publish. `129,341,157.6700 / 2,800,504.4000 / 474,044`, gold rows identical to every other cell (dim_party 118,000, fct_sales 474,044, fct_revenue_summary 119, fct_orders 247,500) — **and identical to this cell's own v0.2.0 run to the last digit**, which is what core 0.3.0's claim that the Spark path does not move looks like from the outside. **DoD 4 is met and `compare_products` exits 0 FIVE ways** — a `--databricks-airflow` flag was added for it. **What it ran against, named rather than implied:** core **v0.3.0** in the worker (asserted from the container, not the lockfile), the *published* `databricks-emulator` **0.2.5**, Sail **0.30.0**, spark-agent **0.30.0**, Unity Catalog **v0.5.0** — pulled, not built here, and **the Jobs cell's pins exactly**, which is the whole design of this cell: same steps, same engine, same pins, different orchestrator. It changed nothing. **Preconditions read, not assumed:** delta directory, emulator `data/` and the PAT all deleted first, and the ERP watermark stood at **93,571** — exactly one seed (G21). *One untidiness, recorded rather than hidden:* unpausing the DAG started the scheduled run alongside a manual trigger, and the duplicate was cancelled mid-flight, so the run list shows a `failed` manual run that is an operator cancellation and not a pipeline defect. **Silver stays on the PySpark runner and the credential stays a file**, deliberately — changing either alongside the orchestrator would leave two candidate explanations for any difference in the numbers. Six deployment facts moved into the platform where they belong (`CONTOSO_PRODUCT_DIR`, `DATABRICKS_TOKEN_FILE`, `CONTOSO_STATE`, `CONTOSO_SNAPSHOT`, `COSMOS_DBT_VENV`, the vendors by service name) plus a TCP bridge, so the product's own code is byte-identical to the Jobs leaf's bar **one line** (`STATE`, and its docstring says why). Gaps it carries: **G28**, **G29**, **G30**, **G31** (Unity Catalog's unbounded JVM heap), **G32** (cosmos venv race — the shared venv was reused across all nine gold tasks this run, `Checked 2 packages in 10ms`), **G33** (`databricks-connect` and the literal name `localhost`), **G34**, **G35** **THE EMULATOR STARTS NOW (G48 closed: a root-owned bind mount, not the image), and this cell had no unattended run until 2026-08-21.** **Verifies itself unattended and green as of 2026-08-22** (G47, G48 and G49 all closed). **The green now asserts the NUMBERS too (G50 closed):** the acceptance run holds this cell's `product_snapshot.json` to `contoso_product.EXPECTED` and printed `129341157.6700 / 2800504.4000 / 474044` unattended. Whether that ✅ still holds is this cell's owner to decide, and it cannot be decided until `verify` prints the failed task's log.
