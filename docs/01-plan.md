# 01 — The plan

The state of the matrix in [`00-family.md`](00-family.md), and the order the
empty cells get filled. **This file changes; that one should not.** When a
cell turns green, the PR that did it updates the row here and points at the
snapshot.

_Last updated: 2026-08-21._

## Where every cell stands

One line per cell: its state, what its last witness ran and produced, and a
link. Everything a row used to carry is in [`history/`](history/), verbatim
and newest first; the row is the answer and the history is the working.
Gap ownership is the table below this one, not the row.

| cell | product | platform | status | evidence / gap |
|---|---|---|---|---|
| Fabric · Airflow 3 | `contoso-data-product-fabric-airflow3` | `fabric-platform-airflow3` | ✅ | `verify__20260820T110647Z`, 33 tasks, exit 0, on core v0.4.0. Direct Lake semantic model queried back through Power BI: `129,341,157.6700 / 2,800,504.4000 / 474,044`, each DAX == SQL on the same run. [history](history/fabric-airflow3.md) |
| Fabric · notebooks + pipelines | `contoso-data-product-fabric-notebook-pipelines` | `fabric-platform-notebook-pipelines` | ✅ | Re-witnessed on a torn-down, rebuilt stack: 16/16 steps, `PASS=61 ERROR=0`, 11/11 catalog checks, 25 tables and 34 lineage edges, `129,341,157.6700 / 474,044`. [history](history/fabric-notebook-pipelines.md) |
| Fabric · built-in Airflow | `contoso-data-product-fabric-airflow-builtin` | `fabric-platform-airflow-builtin` | ✅ | 11/11 tasks on Fabric's own scheduler, `PASS=52 WARN=0 ERROR=0`, `compare_products` exit 0 four ways on `129,341,157.6700 / 2,800,504.4000 / 474,044`. All five DoD items checked. [history](history/fabric-airflow-builtin.md) |
| Databricks · Airflow 3 | `contoso-data-product-databricks-airflow3` | `databricks-platform-airflow3` | ✅ | `verify__20260820T090629Z`, 21/21 tasks, gold `PASS=52`, `129,341,157.6700 / 2,800,504.4000 / 474,044`, against `databricks-emulator` 0.2.7, Sail 0.32.0, Unity Catalog v0.5.0. Figures unchanged across the pin bump. [history](history/databricks-airflow3.md) |
| Databricks · Jobs | `contoso-data-product-databricks-jobs` | `databricks-platform-jobs` | ✅ | Gold runs through Databricks Jobs as a `dbt_task`; `gold job SUCCESS`, exit 0, five contracts, `129,341,157.6700 / 2,800,504.4000 / 474,044`, against the published `databricks-emulator:0.2.9` @ `sha256:5e9d14b6`. [history](history/databricks-jobs.md) |
| Snowflake · Airflow 3 | `contoso-data-product-snowflake-airflow3` | `snowflake-platform-airflow3` | ✅ | `verify__20260820T132216Z`, 28/28 task instances, silver `PASS=13`, gold `PASS=52`, `129,341,157.6700 / 2,800,504.4000 / 474,044`, against `snowflake-emulator:0.1.6` and core v0.3.1. Found G45 on its first run. [history](history/snowflake-airflow3.md) |
| Snowflake · Tasks | `contoso-data-product-snowflake-tasks` | `snowflake-platform-tasks` | ✅ | The whole medallion through Snowflake Tasks: `make verify` exit 0 from empty volumes against the published `snowflake-emulator:0.3.1`; bronze 28 tasks, silver 2 (`PASS=13`), gold 2 (`PASS=53`), `129,341,157.6700 / 2,800,504.4000 / 474,044`. [history](history/snowflake-tasks.md) |

`contoso-sources` is **published**: <https://github.com/calvinchengx/contoso-sources> (Apache 2.0, `_data/` materialised not committed). G10 closed.

**A decision was taken here, and it changes what a green family means.** The
Databricks cell builds gold correctly — its aggregates are identical to
Fabric's to the last decimal place — but two of its own contracts fail on an
emulator defect (G8), and `gold.py` refused to publish a snapshot for a table
that breaks its contract. Both behaviours were correct; together they removed
the cell from the comparison the family exists to make.

**Chosen: separate recording a measurement from asserting a pass.** `gold.py`
writes the snapshot **and then** exits non-zero. The snapshot carries a
`contract_failures` field naming each failing contract in dbt's own words, with
an optional platform-supplied `cause`. `compare_products` compares the
aggregates as normal, and exits **non-zero** when any snapshot carries
failures, while still printing the agreement it found.

Why, in one line: evidence is worth recording even when the run that produced
it failed — and what must never happen is evidence recorded *without* the
failure attached, which is exactly the stale snapshot this platform once
published, silently outliving its own fix.

The options not taken, kept because the reasoning matters:

| | option | verdict |
|---|---|---|
| 1 | **Leave it** — no snapshot while G8 is open. | Honest, and it costs the cell its place in the comparison. |
| 2 | ~~**Name it with `dialect_gap`**~~ | **Does not work — checked, not assumed.** For the optional runtimes `dialect_gap` skips comparison entirely, but Databricks is half of the REQUIRED pair: there it only exempts the snapshot from `empty()`, and the aggregates are compared anyway. It would exempt nothing that matters while permanently disarming the one guard that catches a genuine future zero. Its stated meaning — "this runtime genuinely cannot build gold" — is also false here. |
| 3 | **Cast the aggregates in shared gold SQL.** | Green everywhere, and it hides an emulator defect inside product code. |

Two traps this surfaced, both worth keeping:

- **`ABSENT` beats `[]`.** An always-present empty list makes "this runtime
  evaluated its contracts and they passed" indistinguishable from "it never
  checked".
- **dbt overwrites `run_results.json` every invocation**, and `dbt run` shares
  the target directory. Read without checking, it reports the *models*: nine
  rows, zero failures. Believed, that publishes a snapshot asserting no
  contract failures on a run where two failed — the precise false green this
  design exists to prevent. Assert `args.which == "test"` first.

**A green that names no image is not a green.** For several hours the Databricks
cell passed every contract and the three-way comparison exited 0 — against a
locally built `databricks-emulator:g8fix-local` that had never been pushed
(`docker image inspect --format '{{.RepoDigests}}'` → `[]`) while `versions.env`
still pinned `0.2.4`. The measurement was real and the fix was real; the *claim*
"the cell is green" was not, because no one else could reproduce it. That was
G11's shape one tier up — a change that never ships reaches no consumer — and
the family's own discipline is that a green run says which images it verified.

It has since been verified twice, independently, against the published image:
`ghcr.io/calvinchengx/databricks-emulator:0.2.5`,
`sha256:83bd3ed5cd9d3ba1c0218fb1b5ad629f2089c9dd4a1690098b518ae8106a3da4` — a
digest, where the local build had none.

The sequence that turned it into a reproducible claim — **all of it done**:

1. ~~Merge [databricks-emulator#47](https://github.com/calvinchengx/databricks-emulator/pull/47).~~ Merged, `1a4c0ce`.
2. ~~Cut a `databricks-emulator` release.~~ **v0.2.5**, image published and pullable.
3. ~~Bump `versions.env` in `databricks-platform-jobs`.~~ [#10](https://github.com/calvinchengx/databricks-platform-jobs/pull/10).
4. ~~Re-run `make verify` against the **released** image.~~ `PASS=52 ERROR=0`, exit 0,
   three-way `compare_products` exit 0 — on `ghcr.io/…/databricks-emulator:0.2.5`.
5. ✅ **on this axis.** The cell is still 🟡 for a different reason: DoD 3, gold
   runs from a host script rather than through Databricks Jobs (G4). Worth
   keeping the two apart — "we cannot reproduce this" is a different complaint
   from "this is not the orchestrator we named", and only the first is closed.

A NOTE ON HOW THE LAST STEP NEARLY PASSED WRONGLY. Bumping `versions.env` does
not restart a running stack: the file said `0.2.5` while the container was
still the local build, and the two images have different digests and different
layers. A re-verify that skipped `docker compose up` would have reported green
for the released image while never having run it — the same defect as a green
that names no image, one level subtler. Check the image the containers are
ACTUALLY running, not the pin.

## The gaps, and who owns each

Every empty or amber cell is blocked on one or more of these. A gap is named
here so that it is a line item, not a surprise.

| # | gap | blocks | owner | shape of the fix |
|---|---|---|---|---|
| G1 | **Two silvers — PARTIALLY CLOSED.** The dbt silver project is now canonical in core (**v0.2.0**, `silver_dir()` beside `gold_dir()`), and the Fabric Airflow 3 leaf consumes it and carries no copy. **Still open:** (a) `run_silver` (PySpark) is still a SECOND DEFINITION rather than a runner over those models — the "PySpark executes the same SQL" half is not done; (b) **bronze is duplicated too**, which was not in the original framing — core's `run_bronze` (Spark) and the leaf's `bronze.py` (delta-rs) parse and name independently. Two *writers* is legitimate; an Airflow worker has no Spark session. Two *definitions* of the parse rules and table names is not; (c) no silver-metrics equality test exists yet, and one known divergence has to be encoded rather than asserted away: the dbt `silver_customers` names 6 columns where the PySpark one carries all ~101, so `customer_columns` is 101 vs 6 **by design**. | everything | core | finish (a)–(c) |
| ~~G2~~ | ~~**Two repos contain their product.**~~ **Closed.** Both are split and both are witnessed end to end. `databricks-platform-jobs` → `contoso-data-product-databricks-jobs` (`EXIT=0`, `PASS=52`). `fabric-platform-notebook-pipelines` → `contoso-data-product-fabric-notebook-pipelines` (`EXIT=0`, 16/16 steps, `PASS=61`, 11/11 catalog checks, snapshot `129341157.6700 / 474044`) on a torn-down and rebuilt stack. Both platforms take `PRODUCT` as a path to a `./product` mount point and name no product. | — | — | **The Fabric split cost thirteen runs and exposed NINE couplings, none of which the 103 tests, `ruff` or `compose config` could see** — every one was code assuming the product lived inside the platform. Two of them (the Debezium hostname, `reset_topic`) were G13 fallout that had been live and silent for a day. One was a **passing** run over stale models. That is the argument for the acceptance workflow, not for more unit tests |
| G3 | ~~OPEN~~ **CLOSED for the slice.** The Airflow 2 DAG exists and runs the whole medallion; the differences are written into its docstring as a table so whoever widens it gets the list rather than discovering it one exception at a time. Delivery is item files, not a bundle — which brought its own surprise: stale item directories accumulate in the shared DAG volume and a dead item's DAG SHADOWS the live one by `dag_id`, silently running yesterday's graph. Nothing cleans them up. Originally: **Airflow 2.10.5 ≠ Airflow 3.3.1.** Task SDK vs `airflow.decorators`; `Asset` vs `Dataset`; api-server execution vs direct import; DAG bundle vs OneLake sync. | Fabric · built-in Airflow | leaf | a second DAG in the 2.x idiom, in its own leaf; cosmos pinned to a 2.10-compatible release |
| ~~G4~~ | ~~**`dbt_task` not implemented**~~ **CLOSED. Gold runs through Databricks Jobs as a `dbt_task`, witnessed end to end.** The rewrite landed in [contoso-data-product-databricks-jobs#6](https://github.com/calvinchengx/contoso-data-product-databricks-jobs/pull/6): the project is uploaded to the workspace and run as a job task, and the contract verdict comes from `run_results.json` off the Jobs API. **The three consumer requirements this row recorded were all met, and the third one was where the work actually was.** `dbt run` and `dbt test` run in one task in order, which is stronger than separate invocations: the task stops at its first failing command, so `args.which == "test"` now proves the models built AND the contracts ran. `run_results.json` comes back as a task artefact. And a failing `dbt test` must exit non-zero while still having produced its artefacts — **which turned out to be impossible as specified**: the `SystemExit` that carried the non-zero exit made the fabric statement agent close the connection without a reply, so the artefacts printed before it were lost anyway and the caller saw a transport EOF. The requirement was right and the mechanism could not deliver it; the failure is now a FIELD in the same envelope as the artefacts, where no agent's exception handling can separate the verdict from its evidence. Two further emulator defects had to go with it — the profile carrying the client-facing origin, and `spark_env_vars` silently dropped — all three in [#71](https://github.com/calvinchengx/databricks-emulator/pull/71), released **v0.2.9**, plus the profile-name coupling in [#68](https://github.com/calvinchengx/databricks-emulator/pull/68) (**v0.2.8**). | Databricks · Jobs (DoD 3) | closed | **the lesson is where the gate pointed, not the code**: every one of these was live for as long as `dbt_task` has existed, and CI was green throughout, because `ci:e2e-dbt-task` runs against an agent no platform uses ([#72](https://github.com/calvinchengx/databricks-emulator/issues/72)) |
| G5 | ~~**Snowflake bronze and silver**~~ **CLOSED, and witnessed from published artefacts.** The cell had no source systems at all: it seeded seven empty silver tables and built gold over them, so it could report shapes but never evidence. It now stands the four vendors up from `contoso-sources` on its own port block, pulls them over their real transports, builds bronze by `COPY INTO` from a mounted internal stage — **580,719 rows across 8 tables** — and runs core's silver project unchanged. The two emulator blockers ([#24](https://github.com/calvinchengx/snowflake-emulator/issues/24), [#20](https://github.com/calvinchengx/snowflake-emulator/issues/20)) were **one defect rather than two**, and this row's original framing was right about the symptom and wrong about the cause: the pinned duckdb CLI EXITS 0 AFTER REFUSING a statement, so every unparseable statement came back as `status: ok` and eighteen constructs were hidden that way. Fixed and released in `snowflake-emulator` **v0.1.4**; the dialect macros are core **v0.3.0**. Final measurement on **v0.1.5** and core **v0.3.1**, from empty volumes: silver `PASS=8`, gold `PASS=9`, `129,341,157.6700 / 2,800,504.4000 / 474,044`. What remains is not this gap: the pipeline evaluates no contracts (**G41**). | Snowflake · Tasks, and Snowflake · Airflow 3 inherits it | leaf + emulator | — done |
| ~~G6~~ | ~~**Snowflake Tasks — the consumer half**~~ **CLOSED. The medallion runs through Snowflake Tasks, witnessed from empty volumes against the published image.** This row read the documentation carefully and still got the balance wrong: it said the emulator was largely done and *"the remaining work is mostly the EMULATOR again"* only for `EXECUTE DBT PROJECT` and the manual task. Both were true, and **four more emulator changes were needed that nothing here predicted**, because every one of them was 🟢 on the parity table while being broken. The row's own open question — *"if EXECUTE DBT PROJECT succeeds as a statement while dbt failed, does a task graph run its downstream nodes anyway? Answer that against real Snowflake FIRST"* — **was answered, and the answer was that Snowflake had already changed it**: since October 2025 dbt errors *"appear as query failures"*, noted as a breaking change *"for anyone relying on the previous method of checking the return values"*, and done *"to make it easier to handle them with tasks"*. So the trap this row feared was real and the vendor closed it in the direction this family argues for. The consumer requirements it recorded all hold: `run` and `test` are separate task nodes, `run_results.json` comes back (from the STAGE, by `GET`, which is better — a failed run has no result set to carry evidence and this way it outlives the failure), and a failing `dbt test` fails its node and SKIPS the rest. | Snowflake · Tasks (DoD 3) | closed | **what generalises**: three of the six defects only failed on a SECOND run, and one was the emulator being STRICTER than the tenant — a project a real account accepts, refused here, which sends a consumer looking for a fault in its own code |
| G7 | **`money_is_never_stored_as_float` read SQL Server's `INFORMATION_SCHEMA`.** Empty in Unity Catalog, so it passed vacuously on Databricks — while that engine's money columns read as `double`. **Fixed and released** in core **v0.1.1**: `macros/reflect.sql` (`INFORMATION_SCHEMA` on Fabric, `DESCRIBE TABLE` on Spark engines) and the contract rewritten over it. **Witnessed on Databricks**: `PASS=50 ERROR=2`, and it caught **12** genuinely-float money columns across all five gold models — the red predicted here, for the reason predicted. | contract honesty | core | witness on Fabric; Snowflake once G5 lands. Stays red on Databricks until G8 is fixed upstream. |
| G8 | ~~OPEN~~ **CLOSED — fixed, released and WITNESSED ON THE RELEASED IMAGE.** `make verify` on a clean stack against `ghcr.io/calvinchengx/databricks-emulator:0.2.5` (pulled, not built locally) reports `PASS=52 WARN=0 ERROR=0` with `money_is_never_stored_as_float` among the passes, and the gold star's Delta log now holds `decimal(19,4)` / `decimal(29,4)` where it held `double`. `KNOWN_CAUSES` in `gold.py` is emptied in the same change that made it false (`databricks-platform-jobs` `fc1704c`). Released in databricks-emulator **v0.2.5** ([#47](https://github.com/calvinchengx/databricks-emulator/pull/47), notes in `docs/release-notes/v0.2.5.md`). The consumer's pin moves in [databricks-platform-jobs#10](https://github.com/calvinchengx/databricks-platform-jobs/pull/10), by hand — see G16 for why nothing dispatched. The root cause is not the DOUBLE mapping — that was a symptom of registering column metadata into UC **at all**. Measured four ways over the same Delta bytes: no columns → Sail reads the Delta log and gets `decimal(19,4)`; DOUBLE columns → Sail binds the column as double; `decimal(19,4)` columns → Sail refuses (`Unsupported complex type`); a `_col` placeholder → the table is unreadable. The fix registers no columns and lets the Delta log be the schema, which it already was. Originally reported here as **Decimal columns register in UC as `DOUBLE`, so they are read as float** — `databricks-emulator` `internal/sqlshim/shim.go` (`sparkToUC`) deliberately maps `decimal(p,s)` to `type_name: DOUBLE`; the Delta log, the Parquet physical type and `DESCRIBE` all still say `decimal(19,4)`, but the planner trusts UC. **Not** a `sum()` defect — that was this plan's earlier reading and it is wrong: a fresh `CREATE TABLE t AS SELECT CAST(1.5 AS DECIMAL(19,4)) AS m` answers `typeof(sum(m))` with `decimal(29,4)`, correctly. | Databricks money columns; G7 staying red | **databricks-emulator** ([#46](https://github.com/calvinchengx/databricks-emulator/issues/46)) | the emulator maps it that way because Sail's unity provider rejects `decimal(p,s)` (`Unsupported complex type`), so the real fix is likely upstream in Sail. The databricks snapshot casts back to `DECIMAL(19,4)` in the engine meanwhile. |
| ~~G24~~ | ~~**The platform hand-writes the source systems instead of generating them.**~~ **Closed.** `fabric-platform-airflow-builtin` generates its vendor stack from `contoso-sources/sources.yaml` and tracks no vendor definition — 215 lines out of compose, and the MOKAPI/REDPANDA/DEBEZIUM pins out of `versions.env` because those images are what the vendors ARE. **The drift the copy had is measured, not asserted**: all three simulators carried the POS system's `2GiB/4g`, so the vendor whose whole export is 4 KB held a 4 GB ceiling; it now takes the declaration's `256MiB/512m`. `restart: unless-stopped` moved to the declaration, where the three sibling platforms running that vendor can see it. The ERP topic had never been passed at all, so the DAG was using its own hardcoded default rather than the declaration's. **Three defects surfaced only because this platform was torn down for the first time**: the platform's own no-product test caught a `db_user` defaulting to a product name; `make down` required the generated fragment, so a clean checkout could not tear down; and the sidecar built its TLS bundle under a bare `if [ -f ]` with **no `depends_on` for the emulator**, so on empty volumes it silently never built and `provision` failed four steps later naming neither TLS nor ordering — invisible until now because every prior start found a certificate an earlier run had left. Witnessed from empty volumes: 11/11 tasks, `compare_products` exit 0 four ways. | — | — | The `health` route the declaration carries is now used too: mokapi without its fixture GENERATES bodies from the OpenAPI schema and answers everything 200, so the probe sends a deliberately wrong key and treats 401 as healthy — no secret needed, and all three vendors assert enforcement for the first time |
| ~~G25~~ | ~~**The leaf pins core by published wheel URL, not by tag.**~~ **Closed — not a gap.** Fabric's built-in Airflow is part of `fabric-emulator` because REAL Fabric ships the same built-in Airflow; the image is the vendor's and carrying no `git` is a property of it, not a shortcut this leaf took. A release wheel URL is the same release and the same immutability as the tag it comes from. DoD 2 now says so. | — | — | — |
| G26 | **A vendor grows across runs until it is OOM-killed at its own ceiling.** `contoso-pos` was killed twice at `mem_limit: 4g` — the second time with the host at 3.3 GiB of 15.7 and only 13 containers up, so the first run's explanation (contention across 24 containers) was wrong. Thirteen containers and ample free memory means it hit its own limit after ~45 minutes and many runs, which is growth across runs — the thing `--event-store-default-size=1` is supposed to bound. **The policy now lives in `contoso-sources/sources.yaml`** (G24), so every platform running this vendor gets it rather than the one that happened to be bitten. It keeps a dead vendor from ending a run and is a SEATBELT, not a fix: it converts a leak into an intermittent failure that a green pipeline hides. | any platform running this vendor; `contoso-pos` | **contoso-sources** | find what accumulates in mokapi across requests when the event store is already bounded to 1. Recorded as a row rather than left as a comment in one platform's compose, which is where it was — DoD 5 |
| ~~G27~~ | ~~**A published DAG can run as the previous version of itself, and report success.**~~ **CLOSED — fixed, released and WITNESSED ON THE RELEASED IMAGE.** [fabric-emulator#328](https://github.com/calvinchengx/fabric-emulator/pull/328), released as **v0.31.0**; the pin moved and the platform's 45s sleep is deleted. `make witness` on a cold stack against `ghcr.io/calvinchengx/fabric-emulator:0.31.0` — **pulled, not built here**, `sha256:70b26056` — reports 11/11 tasks and `129341157.6700 / 2800504.4000 / 474044`, with `compare_products` exit 0 four ways. **The whole cycle is 5:01 with no wait at all.** **The sleep could never have worked**: `PUT .../files` only stores bytes on the item, and the write into the scheduler's DAG folder happens in the *Run* handler immediately before the trigger — it slept before the files existed on disk. The race was entirely inside the emulator. **Two plausible signals were measured and rejected**: `last_parsed_time` came back **20 ms ahead** of a write whose content that parse had not read, and `/dagSources` matched disk **13 s** before the task structure changed. The cause is Airflow's `min_serialized_dag_update_interval` (30s), which lets a parse skip rewriting the serialised DAG that task instances come from — **so the 15s-vs-45s history was two guesses either side of a threshold nobody had identified**, which is why one appeared to work. | — | — | **The first fix passed its unit test and did nothing**, because the test was written against the same wrong theory as the code; only measuring a real scheduler settled it. And a test asserting the pin is ≥ 0.31.0 stayed RED through every step until the release existed, which is the only reason the sleep's removal and the bump could be trusted to land together |
| G9 | **Stale Spark Connect session never re-established** by the shared agent (`session … is not running`). `agent.py` calls `getOrCreate()` at module import and holds that one session for the life of the process, so a dropped session is permanent and no caller-side retry can help. Restarting the agent clears it. Bit us twice in one day, between silver and register. | any Sail-backed cell, intermittently | **fabric-emulator** ([#312](https://github.com/calvinchengx/fabric-emulator/issues/312)) | rebuild the session on session-not-found rather than treating `getOrCreate()` as an import-time constant |
| ~~G10~~ | ~~**`contoso-sources` unpublished.**~~ **Closed** — published Apache 2.0, `_data/` still materialised rather than committed. | — | — | — |
| G11 | **Core is consumed by tag, so nothing in it reaches a cell without a release.** v0.1.1 shipped G7's fix; the plan had no step that says so, and a core commit that is never released is invisible to every consumer. | every cell, silently | core | tag `v*` → the release workflow builds and attaches the wheel; then bump each consumer's pin and relock. Four consumers, all pinned as of today: three by wheel URL and one by git tag. `snowflake-platform-tasks` was the exception — it pinned core by **sibling path**, so it could not be cloned alone and no release could reach it; v0.1.1 and v0.2.0 both went past it silently. Fixed. **v0.2.0 exercised the rule for the first time**: the leaf's tests failed with `ImportError` on `silver_dir` until core was released and the pin bumped. It works. **The rule has a second failure mode, found at databricks-emulator v0.2.5 and recorded as G16**: releasing is not enough if the thing that tells the consumer cannot fire, and unlike the `ImportError` above, that one fails SILENTLY — the consumer keeps passing on the old pin. |
| G12 | **The ERP password is the one fixture credential written into tracked files.** The vendor API keys get this right: `_data/**/.api-key` is gitignored and written at `make sources` from the generator (`materialise_sources.py`), so the value is knowable but never committed. `contoso-erp-dev` is not — and it is worse than a single declaration in `sources.yaml`, because each platform generator carries it as a **fallback default** (`v.get("db_password", "contoso-erp-dev")`). Rotating the declaration would leave stale defaults in three platform repos that keep working, so nothing would fail to tell you. | nothing — it guards a throwaway container and is public by construction | `contoso-sources` + every platform generator | generate it at `make up` and write it where both the compose env and the connector config read it, the `.api-key` pattern. **Not a history rewrite**: the value has to be knowable, it is already in four public repos including a tag consumers pin, and rotating forward is the proportionate response |
| ~~G13~~ | ~~**One platform carries its own copy of the vendors.**~~ **Closed.** `fabric-platform-notebook-pipelines` now generates its vendor stack from `contoso-sources/sources.yaml` and tracks no vendor definition. Per-vendor tuning moved into the declaration (`health`, `memory`, `mem_limit`) because a budget sized to a 95 MB export is a fact about the vendor; the generated fragment reproduces the old file's ports and limits exactly. The 13 vendor invariants moved to `contoso-sources`, which now has a test suite; the 3 that assert a consumer's parsing DDL stayed with that DDL. | — | — | — |
| ~~G14~~ | ~~**Core silver uses Spark-SQL constructs.**~~ **Closed, and it was smaller than stated.** The row implied a dialect rewrite; measured, six of the eight silver models port untouched and exactly **three constructs** needed macros — an array explode, a day offset and a date series. They now sit behind `silver/macros/dialect.sql`, following the pattern gold already used (`flag`, `date_quarter`, `varchar_n`). The PySpark renderer learned all three and emits the Spark branch, verified by rendering both models and finding `posexplode`, `date_add` and `lateral view explode` where they were: the change moved where the dialect LIVES, not what Spark executes. | — | — | — |
| ~~G15~~ | ~~**The two runtimes publish the product under DIFFERENT domains.**~~ **Closed.** `contoso-commerce` wins; `fabric-platform-notebook-pipelines` imports `DOMAIN` from core and no longer names its own (`6a1a75c`). `GLOSSARY` and the domain's `displayName` follow it — GLOSSARY stays local because core does not name it and the Databricks runtime publishes no glossary at all, but its value has to track the domain or the catalog reads as two things again. A test fails on either half of the breach: a local `DOMAIN` assignment, or the import going missing. | — | — | **Residual, deliberately not folded in:** the two platforms still PUT the same domain with different `description`s, so the last runtime to run wins that field. And fabric publishes a `dataProducts` entity named `contoso-sales-star` that Databricks does not publish at all, while core names the product `contoso-analytics`. Neither is a two-domains bug; both are core-naming questions |
| G16 | **The Databricks release cannot tell its consumer.** `databricks-emulator`'s `release.yml` fires the acceptance `repository_dispatch` only when `ACCEPTANCE_DISPATCH_TOKEN` is set, and that repository has **no secrets at all** (`gh secret list` is empty; `fabric-emulator` has the token, dated 2026-08-03). So the step logs a warning and exits 0. Its warning is itself wrong: it says the consumer's daily cron still verifies the pin, but on a `schedule` run `github.event_name` is not `repository_dispatch`, so BOTH the step that moves the pin and the step that adopts it are skipped — the cron verifies whatever is already pinned and never moves it. v0.2.5 would have sat unconsumed indefinitely. (Not behind a green board, as first written here: the consumer's cron was RED on 2026-08-17 and 2026-08-18, at `govern.py` on a 400 from OpenMetadata `/api/v1/domains`, since fixed by `f425834`. The release workflows were green. The pin would have gone stale behind a failure that had nothing to do with it, which is worse rather than better.) **The same hole exists on `snowflake-emulator`**, confirmed when v0.1.2 shipped: its dispatch step logged the same warning and its consumer is still pinned at `0.1.1`. Found while bumping 0.2.4 → 0.2.5; that bump is hand-written as a result. | every databricks-emulator release reaching its consumer, silently | **owner: a human** — needs a PAT with `contents:write` on `calvinchengx/databricks-platform-jobs`, which no agent can mint | add `ACCEPTANCE_DISPATCH_TOKEN` to `databricks-emulator`. The adopt path itself is now safe to trust: [databricks-platform-jobs#9](https://github.com/calvinchengx/databricks-platform-jobs/pull/9) made it move `versions.env`, `pyproject.toml` and `uv.lock` together, where before it committed only the first — and because every target runs `uv run --frozen`, which reads `uv.lock` and never `pyproject.toml`, the dispatch would have verified the NEW image against the OLD client and passed. Fix the warning's text too. |
| G17 | ~~OPEN~~ **CLOSED — [fabric-emulator#320](https://github.com/calvinchengx/fabric-emulator/pull/320) merged (`8ff0b72`, 93/93 checks).** Witnessed twice since: the emulator's own e2e, and the built-in-Airflow cell running `provision` and `land` concurrently. A second fix rode along — [#322](https://github.com/calvinchengx/fabric-emulator/pull/322) (`d26f9a4`): a DAG-SYNC failure and a DAG that failed both reported the bare `AirflowRunFailed`/"The job failed.", so an emulator that could not WRITE the DAG files looked identical to a broken graph. Found by a consumer platform's first run. Originally: — the sidecar now runs `CeleryExecutor` over Postgres with a Redis broker, and `DAGS_ARE_PAUSED_AT_CREATION` matches Fabric's `False`. **Witnessed, not configured:** the e2e's witness DAG had ONE task, and one task cannot distinguish the two executors — which is exactly why this survived a green e2e. It now fans out to three branches that record their own windows, and the consumer asserts the wall span against the sum of the work: `ran 3 branches CONCURRENTLY (3.0s wall for 9.0s of work)`. Under Sequential that reads `9.0s for 9.0s` and fails. A `test_airflow_sidecar_parity` pins the shipped sidecar and the e2e's own copy together — they drifted *during* the change. Original finding: **the emulator's built-in Airflow ran `SequentialExecutor`; real Fabric runs `CeleryExecutor` and forbids changing it.** `fabric-emulator`'s profile-gated sidecar sets `AIRFLOW__CORE__EXECUTOR: SequentialExecutor` over sqlite (`docker-compose.yml`, and the same in `e2e/airflow/docker-compose.yml`). Microsoft's docs give the Fabric default as `CeleryExecutor` and list `AIRFLOW__CORE__EXECUTOR` among the configurations a user **cannot override** — so on real Fabric it is *always* Celery and a DAG author has no way to change it. Sequential runs ONE TASK AT A TIME, so the Contoso DAG's four parallel vendor ingests and seven concurrent silver models would serialise: the run would pass while demonstrating behaviour no Fabric user can have. A witness DAG of one task cannot see this, which is why the existing e2e is green. **Version parity itself is already exact** and was checked rather than assumed: Fabric supports Airflow **2.10.5** on Python **3.12** ([concepts](https://learn.microsoft.com/en-us/fabric/data-factory/apache-airflow-jobs-concepts), docs updated 2026-06), and the emulator runs `apache/airflow:2.10.5-python3.12`. No Airflow 3.x in Fabric and none announced. Two smaller drifts alongside it: Fabric defaults `DAGS_ARE_PAUSED_AT_CREATION` to **False** where the e2e sets `"true"`, and `DAG_DIR_LIST_INTERVAL` to **5**. | Fabric · built-in Airflow being a fair demonstration rather than merely green | **fabric-emulator** | give the sidecar `CeleryExecutor` with a real metadata DB and broker, as the family's other Airflow stack already has; align the two defaults above. Then a parallel DAG proves parallelism instead of hiding its absence |
| ~~G18~~ | ~~**snowflake-emulator answered every column as `text`.**~~ **Fixed — [snowflake-emulator#12](https://github.com/calvinchengx/snowflake-emulator/pull/12), `dc5061b`.** `writeQueryOK` typed all columns `text` and emitted every value as a JSON string, whatever DuckDB held, so **all 52 gold contracts ERRORED** on `'0' is not of type 'integer'` — dbt's own result schema rejecting a string where a test's `failures` must be a number. `PASS=0 ERROR=52`: not one verdict, pass or fail. Chasing it found three more defects in the same function, same root cause (`[]map[string]any` plus `fmt.Sprint`), each measured against a running emulator. **Column order was randomised** by Go map iteration — `SELECT 1 AS alpha, 2 AS bravo, 3 AS charlie, 4 AS delta` came back in FOUR orders across twelve identical requests. Reading by name was safe; reading `data[0][0]` is how the platform reads its own gold aggregates, so revenue and cancelled revenue could swap silently. The same bug read `DESCRIBE` positionally, so `DESCRIBE TABLE silver_orders` was right twice in eight tries and `['<nil>','<nil>']` or `['INTEGER','YES']` the rest — **and core's `money_is_never_stored_as_float` reflects types through that statement**. **BIGINT lost digits** to a float64 round trip (`9223372036854775807` → `9.223372036854776e+18`). **NULL became the string `"<nil>"`.** After: `DECIMAL(19,4)` → `fixed` precision 19 scale 4, BIGINT exact, NULL as JSON null, column order 12/12, DESCRIBE 8/8, and **`PASS=52 WARN=0 ERROR=0`**. | every Snowflake contract | **snowflake-emulator** | — **What this does NOT claim**: those 52 pass over an EMPTY silver, so verdicts are now possible, not the data right. That is G5. One is non-vacuous even so — `money_is_never_stored_as_float` reads the catalog, and `fct_revenue_summary` has three money columns, all `DECIMAL(38,4)`. **Needs a release to reach the consumer** |
| ~~G19~~ | ~~**snowflake-emulator was 13 commits behind its own release, and could not build for arm64 at all.**~~ **Closed — released as v0.1.2, 2026-08-19.** `0.1.1` published `linux/amd64` alone, so `make up` died on Apple Silicon with `no matching manifest for linux/arm64/v8`. The tag note said why — *"until DuckDB's arm64 zip name is settled"* — and the Dockerfile had guessed wrong: DuckDB v1.2.2 publishes `duckdb_cli-linux-aarch64.zip` (200), not `duckdb_cli-linux-arm64.zip` (404). One word. The staleness half was G11 again: `main` carried the `SHOW FUNCTIONS` shape dbt-snowflake requires before it will run a model, and honest failure for unparseable SQL — **the pinned `0.1.1` answered `success: true` with `rowset [["ok"]]` to `THIS IS NOT SQL AT ALL`**, a silent 200 against the emulator's own doctrine. **CORRECTION, 2026-08-19: this row credited `main` with fixing that, and `main` had not.** The comparison was a `0.1.1` CONTAINER against a main-branch HOST BINARY, and the difference belonged to neither: the duckdb CLI the Dockerfile pins (v1.2.2) EXITS 0 AFTER REFUSING a statement, while a newer homebrew one exits 1. So the released image kept answering `status: ok` to every statement it could not parse — TASK, STREAM, time travel, TO_DATE, eighteen constructs in all — through v0.1.2 and v0.1.3. Actually fixed by reading stderr rather than the exit code ([snowflake-emulator#13](https://github.com/calvinchengx/snowflake-emulator/pull/13)), verified in a built container. Two builds compared, one conclusion drawn, and the conclusion was about neither — the same mistake the Databricks cell was held to account for one row up. Verified after release by pulling the published image: `Architecture: arm64`, `/health` → `{"status":"ok"}`. | — | — | **Residual:** the consumer is still pinned at `0.1.1` and nothing will move it — see G16, which applies to this emulator too |
| G21 | **Re-running a stack is not idempotent, and the guard that should have made it so was silently dead.** The Fabric cell HAD a `reset_topic()` — with a comment explaining why a re-run must start from a clean stream — but it `docker exec`ed a container it named literally (`…-redpanda-1`) with `check=False`. G13 renamed that service to `…-contoso-erp-broker-1` and it did nothing, silently, for a day: every re-run replayed onto a topic that still held the last, the watermark gate failed at exactly 2 × 93,571, and the message blamed Debezium. **Fixed** in the Fabric leaf: deletes through the broker's admin API using the bootstrap the consumer already has, waits for the log to actually go, and raises. Two faults there, and the quieter one was worse — a container name is deployment knowledge a product should not hold, and swallowing the failure is what let it be wrong unnoticed. | re-runnability of any cell with a CDC vendor | platform + leaf | the Databricks leaf still needs the same treatment: its `make up` after a stop doubles the stream the same way, measured at 187,142 against 28,800 rows |
| G22 | **Repeated `make verify` against ONE long-lived stack accumulates state that no code change can fix.** Three kinds, all measured: the emulator's clock advances **cumulatively** 1200s per run until every token Entra mints is born expired (`401 Unauthorized`, which reads as an auth fault — `schedule.py`'s own comment predicts exactly this); a dropped Spark Connect session the agent never re-establishes (G9, third occurrence today and the first outside Databricks); and an environment bound to the agent, which cannot isolate a second. | anyone iterating locally; NOT CI, which tears down every run | — | not a defect to fix so much as a fact to publish: a trustworthy verify starts from `make down`. It is why the acceptance workflow's clean-stack run is the only one whose green means anything |
| G23 | **A nested `env_var()` default is mandatory, not a fallback.** Core's `gold/models/sources.yml` writes ``env_var('CONTOSO_SILVER_DATABASE', env_var('LAKEHOUSE_ID'))``. Jinja evaluates arguments EAGERLY, so the inner call runs whether or not the outer variable is set — `LAKEHOUSE_ID` is required and dbt says `Env var required but not provided: 'LAKEHOUSE_ID'` while the variable meant to make it unnecessary sits in the environment. Invisible to every existing cell because they all set both; found by the first consumer that set only what the file appears to ask for. | any new gold consumer | **core** | give the inner call its own default, or restructure. Needs a release to reach consumers (G11) |
| G20 | **Stale `ApacheAirflowJob` item directories shadow the live DAG.** The emulator's `SyncDAGs` rewrites `{DAGDir}/{itemID}` but nothing removes directories for items that no longer exist. Recreating emulator state mints new item ids while the old files persist in the shared volume, so two directories define the same `dag_id` and Airflow resolves to a stale one. Nothing errors — you simply run an older graph, and `airflow tasks list` shows a DAG missing a task you definitely wrote, which reads like a failed publish. Compounded by ownership: the files belong to the emulator's uid, so an `rm -rf` from the Airflow container fails silently. | any cell using built-in Airflow, intermittently | **fabric-emulator** | remove an item's directory when the item is deleted, or namespace by item so a dead one cannot shadow a live one |
| G28 | **dbt and the Databricks engine cannot share one Python environment, and an Airflow worker is one environment.** `dbt-databricks` 1.12.4 pins `databricks-sdk<0.118`; `databricks-connect` 19.1 needs `>=0.122`. uv proves them unsatisfiable — **verified as a real incompatibility, not a self-imposed pin**, by resolving them together with the version constraint removed. The Jobs cell never meets this: each step is its own job, and its `pyproject.toml` declares `engine` and `dbt` as conflicting groups that are never installed together. A worker has no such boundary. The quiet failure available here was to let uv drop the engine to whatever dbt allows — a change of client library, made invisibly, in the one cell built to isolate the orchestrator as a variable. | any cell running dbt and Spark Connect in one orchestrator process | leaf | resolved for this cell: the worker installs `engine` and the gold tasks build their own dbt venv (`ExecutionMode.VIRTUALENV`). Anything that later needs both in-process needs a real answer |
| G29 | **Cosmos's better-looking test mode silently drops every ODCS contract.** `TestBehavior.AFTER_EACH` renders one test task per model — 9 models × (run, test) — and evaluates **none of the five singular contracts**, because a singular test is attached to no model and a per-model task has nowhere to hang it. The DAG renders clean, runs clean, and publishes no guarantee. Measured, not reasoned about: the rendered task list contained no contract. `AFTER_ALL` renders one whole-suite task that runs all 52. **Second silent-drop on the same path**: with dbt absent at parse time — which it always is here (G28) — cosmos's default `LoadMode.AUTOMATIC` catches the `FileNotFoundError` and falls back to a *deprecated* parser that knows a subset of what dbt knows, again without failing. Both are the family's recurring shape: a guard reporting success while doing nothing. | any cosmos cell with singular tests — **includes the Fabric Airflow 3 leaf, which uses `AFTER_EACH`** | leaf | done here (`AFTER_ALL`, explicit `LoadMode.DBT_MANIFEST`, a manifest builder that refuses to stamp a manifest missing a contract, and a test that fails if the whole-suite task disappears). **Checked elsewhere, and it is not hypothetical.** `contoso-data-product-fabric-airflow3`'s **gold** group passes `AFTER_ALL`, so its five ODCS contracts do run and its `PASS=52` stands. Its **silver** group passes no `render_config` at all — and cosmos's default is `AFTER_EACH` (confirmed in `cosmos/config.py:88`), so core's one singular silver test, `silver_orders_never_holds_a_non_positive_quantity.sql`, **is rendered by no task and has never run in that cell**. **Now FIXED and re-witnessed** — `AFTER_ALL` on that group too, silver `PASS=13 WARN=0 ERROR=0` with the singular test named in the log, and the cell's numbers unchanged. It was left open for one turn on purpose: one line changes it, and if that test had failed a witnessed ✅ would have gone red on a change nobody had re-run. It passed. **The general lesson is not about cosmos:** the comment justifying the default said AFTER_EACH was right for silver *because every test belonged to the model it followed*, which was TRUE WHEN WRITTEN and expired in ANOTHER REPOSITORY when core v0.2.0 added the singular test — silently, because the default fails by omission. The globbing half of this note is now its own row, **G36** |
| G30 | **The two Databricks leaves carry nine byte-identical binding modules.** `landing`, `credentials`, `seed_secrets`, `spark_session`, `sources` and the four vendor ingests are the same file in `-jobs` and `-airflow3`, differing only in import form (checked, not assumed; ruff line-length matched at 88 so a future diff shows divergence rather than reformatting). This is the family's established answer — the two *Fabric* leaves duplicate their bindings too, sharing no code — but established is not the same as right, and two copies agree until the day one is fixed. Core is the wrong home: none of it is a transformation. | both Databricks leaves; the same shape exists across both Fabric leaves | core or a new shared package | decide whether an engine's *binding* deserves a published package the way its *contract* already does (`databricks-target`, `fabric-target`). Deliberately not done unilaterally — it changes the family's tier structure, which `00-family.md` defines |
| G31 | **Unity Catalog is a JVM with no heap cap, so it sizes itself from the whole machine and aborts.** With no `-Xmx` the container's JVM took the host's 15.66 GiB as its ceiling — most of which belongs to other stacks — and under `register`, which creates eight external tables in quick succession, it died with **SIGABRT (exit 134)** and a corretto crash log. Twice, at the same point. **The failure arrives three layers from its cause**: Sail reports `error sending request` to UC, the task reports eight tables FAILED, and nothing anywhere says a JVM died — so the obvious reading is that `register` is broken. The Jobs cell does not meet it because it is usually the only stack running, which makes this a defect that hides on an idle laptop and appears on a busy one. | any cell whose UC shares a machine | platform (capped here: `-Xmx1g`, `MaxMetaspaceSize=256m`, `mem_limit 1536m`) | **unitycatalog** ships no default heap bound; a container image that sizes its heap from cgroup limits rather than host RAM would remove this for every consumer |
| G32 | **Cosmos builds one dbt virtualenv per task, and they race.** `ExecutionMode.VIRTUALENV` with no `virtualenv_dir` creates a fresh venv for every gold task — nine, concurrently, through a single uv cache. They collided: one lost with `Failed to read from the distribution cache` on `python-dateutil`, which surfaces as **a dbt model failing for no reason a dbt log can explain**. The tell is that the same install by hand succeeds every time. Not merely a slowness: it is a nondeterministic red on a graph whose whole purpose is to be compared against another graph. **CHECKED IN THE FABRIC AIRFLOW 3 LEAF, AND IT DOES NOT APPLY** — not because it was fixed there, but because that cell never enters the mode that has the defect. Measured four ways rather than reasoned about: its groups pass no `execution_mode`, cosmos's default is `ExecutionMode.LOCAL` (`cosmos/config.py:455`), every dbt task renders as `DbtRunLocalOperator`/`DbtTestLocalOperator` with no `py_requirements` or `virtualenv_dir` attribute at all, **zero** log lines across the whole run match any venv creation, and `/tmp/cosmos` holds only partial-parse caches — no `pyvenv.cfg`, no `site-packages`. The tasks shell out to the worker image's own `/home/airflow/.local/bin/dbt`. **And the reason is G28.** The Databricks leaf is forced onto `VIRTUALENV` because dbt-databricks and databricks-connect are unsatisfiable together; the Fabric leaf declares `dbt-core`, `dbt-fabricspark`, `dbt-fabric` and `deltalake` as ordinary dependencies that coexist in one environment (verified in the running worker: 1.12.2 / 1.13.0 / 1.11.0 / 1.6.2). **G32 is a consequence of G28**, so any cell that escapes the first escapes the second — and a cell that later adopts `VIRTUALENV` for any reason acquires this with no warning. | any cosmos cell using `ExecutionMode.VIRTUALENV` with parallel model tasks — **not** the Fabric Airflow 3 leaf (checked; see left) | leaf (`COSMOS_DBT_VENV`, one venv built once and reused) | cosmos could default `virtualenv_dir` per-DAG rather than per-task; until then every cosmos consumer must set it, and nothing warns them |
| G33 | **`databricks-connect` skips TLS only for the literal hostname `localhost`.** Its channel builder ignores `use_ssl=false` in the connection string for any other host, including a compose service name. Measured, not inferred: `sc://databricks:8447/;use_ssl=false` from the worker resolves correctly (`ipv4:192.168.158.13:8447`) and fails with `WRONG_VERSION_NUMBER` — a plaintext server being sent a TLS hello. The product must not care: its `spark_session.connect()` is byte-identical to the Jobs leaf's, where `localhost:<published port>` simply is true, and asking a data product to special-case a network topology puts deployment knowledge in the one place the split exists to keep it out of. | any cell reaching a plaintext Spark Connect endpoint by service name | platform (a stdlib TCP forwarder on `127.0.0.1:8447` inside the worker; `socat` is not in the airflow image and would need a root layer) | upstream, `use_ssl=false` should mean what it says |
| G34 | **A dangling Docker network endpoint that cannot be removed, and a host disk at 97%.** Neither is a defect in this family's code and both cost real time, so they are rows rather than log lines. An endpoint for `contoso-pos` survived `docker network rm`, refused `network disconnect --force`, and blocked the default compose project — worked around with `PROJECT=airflow-dbx` rather than restarting the Docker daemon, which would have killed three other sessions' stacks. **Cleared afterwards, and the difference is worth writing down:** `docker network disconnect -f <network> <CONTAINER NAME>` succeeded where the same command addressed by *endpoint ID* had not, so the re-witness ran under the shipped `airflow-databricks` and the workaround is no longer in force. Separately the host was at **1.7 TiB of 1.8 TiB**, which killed postgres, the vendor containers and UC in ways that read as application faults; 43 GB was reclaimed from **build cache and dangling images only** — no tagged image, volume or other session's stack was touched. | the operator, any cell | — (environment) | restart the daemon at a convenient moment; the family's stacks are large enough that disk headroom is a precondition worth checking in `doctor` |
| ~~G35~~ | ~~**Both Airflow platforms' `make trigger` says “and wait” and does not wait.**~~ **CLOSED — both platforms now have `make verify`, byte-identical so the two cells stay comparable.** It triggers with an explicit `--run-id` and watches **its own** run (a scheduled run started beside a manual trigger during the Databricks witness, and "the most recent run" would have reported a verdict for the wrong pipeline); it **refuses to unpause**, printing `make unpause DAG=…`, because unpausing starts a catch-up run beside yours and two runs writing one catalog is how a witness stops being one; **a timeout exits 1 saying it has no verdict**, rather than passing. On failure the tasks that actually FAILED sort above those merely blocked — the first draft printed 26 alphabetically and buried `provision (failed)` thirteen lines under twenty-five `upstream_failed`, the cause reported three layers from itself. **Witnessed both ways**: red on a planted failure, red on a real one (**G37**), and green — exit 0 on 32/32 with silver `PASS=13` and gold `PASS=52`. `trigger`'s help now says what it always did. | DoD 3 for both Airflow cells | platform | done |
| ~~G36~~ | ~~**The Fabric Airflow 3 snapshot names contracts by globbing filenames, not by what ran.**~~ **CLOSED, and the reason it globbed is the part worth recording:** cosmos clones the project into a temp directory and runs there, so `run_results.json` — the only record of which tests an invocation evaluated — was deleted with the clone. There was nothing to read. Fixed in two halves: the gold group sets `DBT_TARGET_PATH` so its artefacts outlive the task, and `snapshot.py` reads the verdict from them — `args.which == "test"` asserted (**not a corner case here**: the nine gold model tasks write to the same directory, and a `dbt run` artefact reports models and zero failures, which believed publishes "no contract failures" for a run whose contracts never executed), a name on disk the run did not evaluate is a hard failure, and `contract_failures` travels with the numbers — absent when clean, never `[]`. **Gold only**: silver writes its own `run_results`, and one shared directory would let silver's verdict be read as gold's. `verdicts()` is a separate function so the logic deciding what this cell CLAIMS is testable without a warehouse; five tests cover it, and one holds the two halves together because a fix spanning two files joined by an env var and a matching default is the shape that drifts. **Re-witnessed cold:** 32/32, silver `PASS=13`, gold `PASS=52`, artefact reads `which = test` with 52 results, numbers unchanged, `compare_products` exit 0 five ways — and all three guards were **watched refusing against the live warehouse**, writing no snapshot file in any case. | Fabric · Airflow 3's DoD 4 | leaf | done |
| ~~G37~~ | ~~**Three gold tasks claim ONE asset, and Airflow fails the losers after their work succeeded.**~~ **FIXED — `emit_datasets=False` on both groups** (`contoso-data-product-fabric-airflow3` branch `fix/g37-cosmos-asset-race`, `ada9b74`), guarded by `test_no_dbt_task_emits_an_asset_cosmos_invented`, which asserts the SHAPE — no dbt-rendered task carries outlets — so a fourth `DbtTaskGroup` added without the knob fails in pytest rather than intermittently in a witness. **The evidence is mechanical, not probabilistic, which is better than the consecutive greens originally proposed:** the race needed cosmos to register assets, and across the whole rebuilt stack's logs cosmos now registers **zero** — `Dynamically creating AssetModel …fabric-warehouse…` appears **0 times**, where the failing run had it **6 times for `dbo/fct_orders` alone**. Plus one full green: 32/32, silver `PASS=13`, gold `PASS=52`. **The second full green has since landed:** `verify__20260820T001405Z`, 16 minutes end to end on a freshly recreated stack (new metadata DB), mount verified at `ada9b74` with a clean working tree, snapshot `129,341,157.6700 / 2,800,504.4000 / 474,044` unchanged — so both named greens (`verify__20260819T231356Z`, `verify__20260820T001405Z`) plus the mechanical zero-registrations evidence now stand together. The runs between them went red and **not one on this race** — every failure was a container killed by the host (**G38**), diagnosed each time from `make verify`'s first line, including one trigger that landed in a metadata DB recreated seconds later, which `verify` correctly refused to report a verdict on. Originally: two gold models went red *after* their own dbt reported `OK created sql table model … PASS=1 ERROR=0` — the tables were built. Cosmos had assigned three concurrent tasks the SAME outlet, so they raced on one unique key; one won and the API server answered the others with `Error updating Task Instance state. Setting the task to failed.` **while their payload said `TISuccessStatePayload(state=SUCCESS)`**. One run in two, invisible until `make verify` (G35) could run the pipeline twice and go red. | Fabric · Airflow 3, **and the Databricks Airflow 3 leaf, which was never safe — only unobserved** | leaf | done in both. **The Databricks leaf was checked and found EXPOSED, not clear:** it never set `emit_datasets`, so it ran with emission on and nine concurrent gold models — the same configuration that failed in Fabric. Two clean runs against a one-in-two race is a one-in-four coincidence, not evidence. With the knob removed its new guard names all nine gold tasks as emitting, so the exposure was real; `emit_datasets=False` closes it. **The first guard was itself the defect it guarded against:** it asserted no rendered task carries `outlets`, but cosmos's own doc says emission happens *during task execution* — a rendered task carries none either way, measured on the pre-fix DAG. It appeared to work only because emission-on crashed the render with `sqlite3.OperationalError: no such table: variable`, the test having omitted the `AIRFLOW__COSMOS__ENABLE_CACHE` monkeypatch its siblings set. A crash in the right direction reads exactly like a working guard. Both leaves now assert `emit_datasets` on the rendered operator — observable, and the value the task actually uses — and both were re-checked on the assertion MESSAGE rather than the exit code. **The Databricks leaf has not been re-witnessed** since the change |
| G38 | **The host kills containers mid-witness, and the emulators have no memory limit.** Docker has **16 GB** and at least four stacks share it — this one, `fab-airflow-builtin`, and a `compose-*` stack running OpenMetadata + OpenSearch. Measured with `docker inspect` rather than inferred: `fabric-emulator` `Exited (137) OOMKilled=true`, and later `contoso-pos` `Exited (137) OOMKilled=true` **during a verify run**. Neither declares a `mem_limit`, so each sizes itself against the whole machine — the same shape as **G31**, where Unity Catalog's unbounded JVM aborted under `register`, and it hides on an idle laptop exactly the same way. **The cost is not just a red run**: recovering with `make up` re-ran the ERP seed onto a live topic and doubled the watermark to **187,142** (**G21**, in the Fabric cell this time), so the only safe recovery was a full cold teardown. Disk sat at **96%** throughout. | any witness on a shared machine | platform / operator | give the emulators and vendors `mem_limit` as `uc` already has (G31); until then a witness needs the other stacks stopped, which is the operator's call and not a platform's |
| G39 | **A healthcheck that can never pass has been reporting `unhealthy` for nine hours while the service worked.** `fabric-platform-airflow3/docker-compose.yml:133` gives `spark-agent` `curl -sf http://localhost:8099/health`, and the image has no curl: the health log reads `/bin/sh: 1: curl: not found`, exit 1, every interval. Silver ran through that service successfully many times in the same window. It is the mirror image of the defect this family keeps finding — a guard that always passes carries no information, and neither does a check that always fails, except that this one also trains a reader to ignore `unhealthy` on a stack where it might one day be true. | `fabric-platform-airflow3`; check the sibling platforms for the same probe | platform | probe with something the image actually has (a python one-liner, as the airflow service's own healthcheck already does) |
| G40 | **Airflow starts scheduling before the vendors exist, so every `make up` manufactures one failed run.** Measured on a cold start of `databricks-platform-airflow3`: the DAG parsed, the scheduler fired `scheduled__2026-08-20T00:00:00` immediately, and `land` died 16 seconds later with `KafkaException: _UNKNOWN_PARTITION — "Failed to get watermark offsets: Local: Unknown partition"`. Debezium had not created `contoso.erp.customer` yet. Nothing is wrong with the pipeline; the platform simply declares no dependency between *Airflow may schedule* and *the vendors are ready*, where it already declares plenty between the containers themselves. **The cost is not the failed run, it is what a red run comes to mean.** A platform that produces a guaranteed red on every start trains its operator to skim past red in the run list — on a stack whose whole value is that red means something (G35 existed because nothing here could go red at all). `make verify` is immune because it watches its own `--run-id`, which is precisely why it does that; a person reading the UI is not. | both Airflow platforms — `fabric-platform-airflow3` schedules the same way and should be checked | platform | either hold the DAG paused until the vendor healthchecks pass and let `make up` unpause it, or give the scheduler a start date that does not backfill into a warm-up window. Not the product's problem: its `land` failed correctly and said exactly why |
| G41 | ~~**The Snowflake pipeline runs no data tests, and publishes five contract names anyway.**~~ **FIXED and re-witnessed cold** ([leaf#3](https://github.com/calvinchengx/contoso-data-product-snowflake-tasks/pull/3)). `make verify` invoked `dbt run` twice and `dbt test` never, while `steps/gold.py` filled the snapshot's `contracts` field with `sorted(p.stem for p in (product / "tests").glob("*.sql"))` — a DIRECTORY LISTING, which named the five ODCS contracts whether or not one had been evaluated. The cell had therefore never asserted a contract through its own pipeline, and its snapshot read as though it had. Third appearance of the family's recurring shape: G29 (cosmos's `AFTER_EACH` renders no task for a singular test) and G36 (contract names from a listing rather than `run_results.json`) are the same defect elsewhere, and all three are fixed the same way — dbt writes down what it ran, so read THAT. **The glob did not disappear, it changed job**: `tests/*.sql` is the EXPECTATION and `run_results.json` is the EVIDENCE, so a contract core ships that dbt did not evaluate is now a failure — precisely the failure a listing can never notice. `dbt test` is a separate invocation from `dbt run` because their exit codes mean different things; `call` rather than `check_call` so a failing test still WRITES the artefact naming which test failed; and `args.which` is asserted because both commands write the same filename, and reading it unchecked would report models built as tests passed. **Guards checked against the code they reject, not assumed to bite**: with the two steps restored from `main`, 2 failed / 11 passed; with the fix, 13 passed. A fourth exercises the checker against a contract that never ran, one that ran and failed, and a `dbt run` masquerading as a `dbt test`. Cold re-run on merged `main`: silver `PASS=13`, gold `PASS=52`, numbers unchanged. | Snowflake · Tasks; Snowflake · Airflow 3 would have inherited it | leaf | — done |
| G42 | **`fct_revenue_summary`'s row count is a function of WHEN a cell runs, and this table has been quoting it as a cross-cell identity.** Snowflake returned **84** rows where the Fabric and Databricks rows here record **119**. Measured rather than argued: the seeded window is 2026-07-01→2026-07-28, 28 distinct order dates, wholly inside **FY27 Q2** — one fiscal quarter, so the quarter dimension of the group key contributes a factor of one. A run whose rolling window straddles a quarter boundary produces roughly double the groups from identical data. The money does not move with it: `revenue_usd`, `cancelled_revenue_usd` and `sale_lines` agree to the last digit across all cells, and `compare_products` — which reads the snapshot — compares exactly those and is unaffected. The risk is this prose: a future cell will be measured against 119, and will either fail for the calendar or pass for it. | every cell whose row here quotes gold row counts | core (`compare_products` / this document) | **PARTLY CLOSED, by evidence rather than by edit.** Snowflake · Airflow 3 ran the same models on the same day and returned **84 as well** -- the first independent confirmation that the number tracks the CALENDAR and not the engine, which is what this row asserted from one observation. The durable fix is still open and is not prose: a snapshot should carry the WINDOW it was built from (`min(order_date)`/`max(order_date)`), and `compare_products` should compare row counts only between snapshots whose windows match. Until it does, a row count quoted here means nothing without the window beside it, and no cell should be failed on one |
| G43 | **A snowflake-emulator release now needs two bumps, in two repositories.** The split moved the `snowflake-target` wheel URL to the product and left `SNOWFLAKE_EMULATOR_VERSION` in the platform's `versions.env`, so nothing in either repository alone can see the pair — and a workspace binary and a client that disagree about the contract is the one mismatch a consumer repository exists to notice. `scripts/check_product_pin.py` runs at the TOP of `make verify`, where the platform HAS been pointed at a product, and refuses when they disagree; the dispatch-driven acceptance run therefore goes RED until the leaf is bumped too. That is deliberate: bumping the checked-out product in place would publish a green for a combination that exists in no commit anywhere. The same unsolved shape exists across both Databricks repositories, where the leaf's pin is moved by hand. | Snowflake · Tasks; both Databricks cells have it unguarded | platform + leaf | a release flow that opens the leaf's bump as its own PR, rather than a red acceptance run standing in for one |
| G44 | ~~**OpenMetadata is pulled from a vendor registry that is not Hub or GHCR, and it times out.**~~ **CLOSED. The images are mirrored into GHCR and all four consumers pull from there.** It stopped being intermittent on 2026-08-22: two of the seven G50 acceptance runs died at `make up` with a TLS handshake timeout, and the rerun nineteen minutes later died again with `connection reset by peer`, so **the bounded retry `fabric-emulator/e2e/governance/stack.py` already carried would not have saved either**. `calvinchengx/emulators` now owns the mirror ([#7](https://github.com/calvinchengx/emulators/pull/7), [#9](https://github.com/calvinchengx/emulators/pull/9)): `mirrors.json` declares each image, `scripts/mirror_images.py` copies it with `buildx imagetools create` and records the digest GHCR serves, and a weekly job re-checks. **`imagetools create` and not pull/tag/push**, because both images are `linux/amd64` **and** `linux/arm64` and a pull resolves an index to the puller's arch -- a mirror made that way stays green on amd64 CI while breaking every arm64 laptop, so the required platforms are recorded and the check fails for it. **The copy is byte-identical**: the mirror's index digest equals upstream's, `sha256:f5cdd8b6...` for the server (the same digest the failing pull named) and `sha256:7c71755e...` for postgresql. Consumers reference the mirror **by tag**, deliberately not `repo:tag@digest`, because docker resolves the digest and ignores the tag beside it, so a version bump that forgot the digest would silently keep running old bytes; the digest lives in the hub, one place for four consumers. **Witnessed end to end**: fabric-emulator's containerized `OpenMetadata governance profile catalogs the emulator` and `OpenMetadata authenticates against entra-emulator` both passed on [#350](https://github.com/calvinchengx/fabric-emulator/pull/350) with `om-postgresql Pulled`, `openmetadata Pulled`, containers healthy, and **zero occurrences of `getcollate` in the job log**. *A prediction that was wrong, recorded because it was published first*: I said a new GHCR package lands private and needs a manual `admin:packages` flip. It did not -- an anonymous token fetched both manifests, HTTP 200, from a machine with no `ghcr.io` credentials. The push job now measures and reports that per package rather than instructing, and the check job reads GHCR with no login so a future private mirror is a red rather than a surprise in someone's `make up`. *And two guards that would have been hollow*: fabric-emulator's pin test found its images with a regex naming `docker.getcollate.io`, and making it registry-agnostic opens a hole -- measured, pointing all three images back at the vendor leaves it green -- so which registry is now its own assertion; and the three platforms got a registry ALLOWLIST rather than a ban on one name, since banning `getcollate` passes the day somebody adds a different vendor registry. `opensearchproject/opensearch` is deliberately not mirrored: it is on Docker Hub, which the family already trusts. Previously: **OpenMetadata is pulled from a vendor registry that is not Hub or GHCR, and it times out.** `make up` failed at `om-postgresql` with `Get "https://docker.getcollate.io/v2/": net/http: TLS handshake timeout`, and passed on rerun — reported by the Snowflake · Tasks session against [run 32370355104](https://github.com/calvinchengx/snowflake-platform-tasks/actions/runs/32370355104). **Four repositories pull that host** (checked, not assumed): `databricks-platform-jobs`, `snowflake-platform-tasks`, `fabric-platform-notebook-pipelines` and `fabric-emulator` itself. Every governance stack therefore has a single-vendor availability dependency that neither Docker Hub nor GHCR backs, and every one of them runs on a nightly cron — so this fails unattended, on a schedule, and reads as a broken governance step rather than as somebody else's registry. **Distinct from G38**: nothing is OOM-killed and nothing is oversubscribed; the bytes never arrive. | — | — | done. The weekly check in the hub is what would notice a mirror going missing or losing an architecture |
| G45 | ~~**snowflake-emulator refused concurrent statements.**~~ **FIXED and released as v0.1.6** ([#38](https://github.com/calvinchengx/snowflake-emulator/pull/38)). Every statement runs the duckdb CLI as a child process against the warehouse file, and duckdb holds an EXCLUSIVE lock for the life of that process. Two statements in flight meant the second was refused with `Could not set lock on file … Conflicting lock is held in /usr/local/bin/duckdb (PID …)` — a valid statement answered with an error, which is the one thing this emulator's doctrine forbids. Real Snowflake serves concurrent queries. **WHY IT SURVIVED, and it is the argument for this cell existing:** every consumer up to now was SEQUENTIAL. The Tasks cell runs one step at a time from a Makefile; the e2e suites issue one statement at a time. Nothing had ever asked the server two questions at once. `contoso-data-product-snowflake-airflow3` was the first that did — cosmos renders eight silver models as eight Airflow tasks and the executor ran them together — and **three of eight failed while five succeeded**, a partial non-deterministic failure that reads like flaky SQL rather than a missing lock. A MUTEX RATHER THAN A POOL, deliberately: pooling would keep the database open across statements, a larger change that would move WHEN writes become visible. Serialising preserves the semantics exactly. Checked against the defect rather than assumed to bite: eight concurrent statements against a seeded file pass with the mutex and fail without it, failing the way the cell did. | every consumer; found only by a concurrent one | **snowflake-emulator** (released) | — done |
| G46 | **Both Snowflake cells advertise `SNOWFLAKE_TARGET=emulator\|real` and neither one's ingest can honour it.** The promise is made in two places — the platform READMEs, and `credentials.py`'s real-target branch, which refuses to read a key from disk on a real target precisely so the code can run against a real account. The ingest half cannot: it gets bytes into the stage by WRITING INTO THE STAGE DIRECTORY, and a real Snowflake account has no such directory. A consumer following the README to a real account reaches bronze and finds an empty stage. **Found by a peer session building the fix, not by either cell's tests** — which is the part worth recording: every test here exercises the emulator path, so the real path is the one nothing runs. The mechanism is the cause; the FALSE PROMISE is the defect. **The fix exists**: `snowflake-emulator` [#40](https://github.com/calvinchengx/snowflake-emulator/pull/40) implements `PUT` through the driver's own file transfer agent — the same code path a real account takes — merged as `963c2ef7`, awaiting a release. **The migration is entangled in one line, and it is not the one that looks entangled.** `bronze.py` issues one `COPY INTO` per NAMED file already, so the copies are fine; but the line above the loop is `parts = sorted((STAGE / subdir).glob("*.csv"))`, which discovers the part list by globbing the stage FROM THE CLIENT SIDE. That is exactly the filesystem access `PUT` exists to remove, and `AUTO_COMPRESS` defaults TRUE as on a real account, so after an upload the parts are `.csv.gz` and that glob matches nothing. Bronze would fail loudly — the `rows == 0` guard is right there — but blaming ingest. The part list has to come from the ingest side, which knows the names because it wrote them. | Snowflake · Tasks and Snowflake · Airflow 3, equally | both leaves | move ingest to `PUT` once #40 is released, take bronze's part list from ingest rather than a glob, then RE-WITNESS both cells. Not done until a run has happened: the whole defect is a promise nothing exercised |
| G47 | ~~**G40's fix works, and it makes unattended verification impossible.**~~ **CLOSED, in both platforms.** The DAG ships paused (G40), so `verify` refused before starting; unpausing alone only moved the refusal to the catch-up run that unpausing creates. Fixed as two halves that had to land together: `verify` ADOPTS a single in-flight run by id, ported from `fabric-platform-airflow3` ([snowflake#7](https://github.com/calvinchengx/snowflake-platform-airflow3/pull/7), [databricks#12](https://github.com/calvinchengx/databricks-platform-airflow3/pull/12)); and `verify` UNPAUSES, behind `VERIFY_UNPAUSE=1`, from INSIDE the target after its own scan wait ([snowflake#8](https://github.com/calvinchengx/snowflake-platform-airflow3/pull/8) `2c72370e`, [databricks#15](https://github.com/calvinchengx/databricks-platform-airflow3/pull/15) `7b9cc278`). **THREE TIMING BUGS IN ONE CODE PATH, all the same shape: assuming a state is settled while it is still converging.** (1) The job's `timeout-minutes` was shorter than `VERIFY_TIMEOUT`, so GitHub cancelled the run before `verify` could say what it was waiting on. (2) A `make unpause` step sequenced before `verify` unpauses nothing, because `make up` recreates the metadata database and the DAG does not exist for tens of seconds; only `verify` waits for the scan. (3) Unpausing does not create the catch-up run synchronously, so reading the in-flight list immediately finds none, triggers a second run, and the scheduler's catch-up lands a second later: two runs writing one catalog, arrived at by fixing the check that exists to prevent exactly that. A bounded `VERIFY_SETTLE` wait closes the third. Proven end to end on both: `unpausing contoso_daily` then `adopting the run already in flight, scheduled__2026-08-22T00:00:00+00:00` then a task-level verdict. | — | — | done. The verdicts it now produces are G49 |
| G48 | ~~**`databricks-emulator` 0.2.9 exits 1 at start-up, and the container's logs are thrown away.**~~ **CLOSED, and THE EMULATOR WAS NEVER BROKEN.** Two changes, in the order that mattered. First make the failure legible: `make up` now prints `ps -a` and container logs before anything tears the stack down ([databricks#13](https://github.com/calvinchengx/databricks-platform-airflow3/pull/13) `8997581b`), because `up` resolves `depends_on` itself and reports only `dependency failed to start`, and CI's `make down` destroys the evidence in its cleanup step. The very first run with logs gave the cause in one line: `databricks-1 \| open /data/identity.json: permission denied`. **The image declares user 65532; Docker creates a missing bind-mount source as ROOT; the emulator's first act is to write its identity.** Fixed by creating and `chmod 0777`-ing the state directories before compose runs ([databricks#14](https://github.com/calvinchengx/databricks-platform-airflow3/pull/14) `d023c275`). **`databricks-platform-jobs` ran the same image green throughout**, because its `compose.py` has done exactly this since the split; this platform drives compose from Make and never picked it up. An asymmetry between siblings that nothing compared, presenting as a broken release. Three CI runs and a release produced nothing but `exited (1)`; the diagnostic change paid for itself within the hour. | — | — | done. The same `compose logs` guard is still missing from the other six platforms, two of which drive compose through `scripts/compose.py` and need it in Python |
| G49 | ~~**Both Airflow 3 cells' pipelines fail, and `verify` names the task but not the reason.**~~ **CLOSED. Both cells now verify themselves unattended, green.** `snowflake-platform-airflow3` [run 32544129267](https://github.com/calvinchengx/snowflake-platform-airflow3/actions/runs/32544129267) in 9 minutes, `databricks-platform-airflow3` [run 32544132548](https://github.com/calvinchengx/databricks-platform-airflow3/actions/runs/32544132548) in 36. **The diagnostic half came first and paid for itself immediately**: `verify` prints the failed task's own Airflow log ([snowflake#9](https://github.com/calvinchengx/snowflake-platform-airflow3/pull/9), [databricks#16](https://github.com/calvinchengx/databricks-platform-airflow3/pull/16)), and the very next run named both causes in one line each, where the previous cycle had taken three runs and a release to reach `exited (1)`. **Both causes were the same defect, and it was neither pipeline's fault**: `PermissionError: '/opt/product-out/product_snapshot.json'` and `no workspace token at /opt/product/data/admin.pat ([Errno 13] Permission denied)`. That is FOUR instances in one evening of one shape, counting the `PUT` destination and `/data/identity.json` (G48): a container runs as a non-root uid, and a host path it mounts was created by somebody else or never created at all. On a laptop the uids often line up and nothing is noticed; on a CI runner they do not. Fixed by deriving the writable bind mounts from `docker compose config` rather than listing them ([snowflake#10](https://github.com/calvinchengx/snowflake-platform-airflow3/pull/10), [databricks#17](https://github.com/calvinchengx/databricks-platform-airflow3/pull/17)), which creates what is missing and widens ONLY what it created: an existing directory belongs to somebody, and `contoso-erp-seed` legitimately mounts the sources CHECKOUT read-write. Databricks additionally needed `chmod 0644` on the token, because `compose cp` preserves the 0600 the emulator wrote it with. | — | — | done. What these greens do NOT assert is the numbers: that is G50 |
| G50 | ~~**No acceptance run in the family asserts the product's numbers.**~~ **CLOSED, both steps, and witnessed on all seven cells.** Core publishes the three cell-invariant figures as data (`contoso_product.EXPECTED`, [#41](https://github.com/calvinchengx/contoso-data-product/pull/41)) and `scripts/assert_snapshot.py` holds one cell's snapshot to them ([#43](https://github.com/calvinchengx/contoso-data-product/pull/43)); every platform's acceptance run now calls it from a SHA-pinned checkout of core, and **each of the seven printed `129341157.6700 / 2800504.4000 / 474044` in its own CI log**: [fnp](https://github.com/calvinchengx/fabric-platform-notebook-pipelines/actions/runs/32549930842), [fab](https://github.com/calvinchengx/fabric-platform-airflow-builtin/actions/runs/32549932149), [fpa](https://github.com/calvinchengx/fabric-platform-airflow3/actions/runs/32549933279), [dpj](https://github.com/calvinchengx/databricks-platform-jobs/actions/runs/32549934347), [dpa](https://github.com/calvinchengx/databricks-platform-airflow3/actions/runs/32549935444), [sft](https://github.com/calvinchengx/snowflake-platform-tasks/actions/runs/32549936898), [spa](https://github.com/calvinchengx/snowflake-platform-airflow3/actions/runs/32549938276). **TWO CELLS WERE WORSE THAN THIS ROW RECORDED.** The finding was "the pipelines write a snapshot and nothing reads it back"; for two of them nothing wrote one. `fabric-platform-notebook-pipelines` has a `make snapshot` target that is not in the product's STEPS list, so no unattended run had ever called it, and `fabric-platform-airflow3`'s DAG never wrote one at all -- its `semantic_model` task read the three figures and discarded them. That cell needed the snapshot logic moved out of `scripts/` into `contoso_airflow.snapshot` and a `publish` task added ([leaf#8](https://github.com/calvinchengx/contoso-data-product-fabric-airflow3/pull/8)), and it worked on its first real run. **The assertion runs from a CHECKOUT of core, not from the product's venv**: no released wheel carries `expected.py`, so the venv route would have held every cell red until seven leaf pin-bumps merged. Pinned by SHA because a nightly whose expectations can move without a reviewed commit in the platform is a nightly another repository can turn green. **Each platform also gained a repo test that fails if the step is removed or the ref unpinned**, and where the snapshot path crosses a container boundary the test derives it from the compose mount rather than restating it. `fabric-platform-airflow3` shipped no `tests/` at all, which its own Makefile recorded as a gap; it has one now. *Two guards were found hollow on the way*: a platform's no-product exemption would have permitted every leaf name had `contoso-data-product` been listed as a plain substring, and the comment claiming that class of defect was "caught by the check below" named a check that did not exist; and in the fabric-airflow3 leaf a test asserted the STRING `CONTOSO_GOLD_TARGET` appeared in a file, which would pass while the two defaults disagreed. Both now compare values. Previously: **No acceptance run in the family asserts the product's numbers. Every nightly proves a pipeline RAN, not that it produced the right answer.** Checked across all six platforms with an acceptance workflow: **zero** of them execute `compare_products`, and none compares a snapshot against an expected value. `compare_products.py` is referenced in comments in four repositories and run by no workflow anywhere. The pipelines DO write `product_snapshot.json`; nothing reads it back. **Every witnessed figure in the table above therefore comes from a run somebody drove by hand**, and the nightlies that look like they renew those figures do not. A cell whose gold silently returned different money would stay green indefinitely, which is the same shape as G29, G36 and G41 -- a check that cannot fail for the thing it appears to check -- one level up from any of them. **This is a design decision and not a sweep, which is why it is a row rather than a patch.** A single platform's CI can only see its OWN snapshot; `compare_products` exists to diff snapshots ACROSS cells, which no one repository can do. **DECIDED 2026-08-22: each cell asserts its own numbers against expected values PUBLISHED BY CORE.** The alternative considered and rejected was a shared snapshot store with one comparing job; the chosen shape keeps a cell verifiable ALONE, which is what a nightly needs. **The first obstacle is that core does not publish them.** The description of core in this document says it carries "transforms, contracts, expected numbers", and the first two are in the package while the third is PROSE: `129,341,157.6700` appears in `docs/00-family.md` and in this file, and nowhere a program can read. `semantic.py` mentions it only in a docstring and derives its own expectations from the run's snapshot. So step one belongs to core and is not a formality: publish the cell-invariant figures as data in `contoso_product`, and generate the prose from that data rather than maintaining both. **G42 constrains WHICH figures may go in.** `revenue_usd`, `cancelled_revenue_usd` and `sale_lines` are cell-invariant and are the whole set. Gold ROW COUNTS are not: `fct_revenue_summary` returned 84 on two cells and 119 on others purely because the seeded window sits inside one fiscal quarter, so publishing a row count as an expectation would fail a correct cell for the calendar. | — | — | done. Every nightly can now fail because the numbers are wrong, which no nightly in this family could do before |
| G51 | **The family scans its emulators and none of its products or platforms.** **One of the sixteen is now scanned, and it was chosen rather than picked off in order.** `contoso-data-product` is scanned as of [#46](https://github.com/calvinchengx/contoso-data-product/pull/46): it is the one repo named here whose code reaches people who never clone it, since every leaf installs its wheel — supply chain where the rest are demos. `contoso-sources` followed in [#3](https://github.com/calvinchengx/contoso-sources/pull/3), and it was the least obvious of the sixteen: a CI checkout is five Python files and three JavaScript ones, and those three are the mokapi `serve.js` scripts implementing each vendor's auth check and paging. Every platform mounts them read-only and every leaf's bronze is whatever they return — **so a defect there is a defect in the inputs all four runtimes are compared against, which is the one failure cross-runtime agreement cannot detect.** All four would be wrong identically and `compare_products` would still exit 0. **Fourteen remain**: the seven platforms and the seven leaves. (`snowflake-emulator` was already inside the nine by the time this row was written — [#60](https://github.com/calvinchengx/snowflake-emulator/pull/60) closed the odd one out among shipped images and is what prompted the survey.) Verified by API rather than by looking for the file — `code-scanning/analyses` answers *no analyses* for each. The bar for those is genuinely lower: they are demonstrations, not artefacts anybody installs. **This is not a hypothetical, and the evidence has since doubled.** `snowflake-emulator` was also unscanned until [#60](https://github.com/calvinchengx/snowflake-emulator/pull/60), and the reason it got one is that `fabric-emulator` IS scanned. A second alert followed days later — **73**, `py/http-response-splitting`, where the docs preview server built a `Location` header from the request path. That one had a real fix rather than a dismissal ([#378](https://github.com/calvinchengx/fabric-emulator/pull/378)): the resolver has exactly one redirect target and it is a build-time constant, so sending the constant removes the flow instead of sanitising it, and the self-test now pins that assumption against percent-encoded and literal CRLF. **Verified closed** (`state=fixed`) rather than assumed from the merge. Three findings from one scanned repo, in a family where sixteen are not. Earlier, alert 71 (`go/reflected-xss`, high) led to two real defects: alert 71 (`go/reflected-xss`, high) led to two real defects — OneLake's DFS error body built with `%q`, which is Go quoting and left `<` `>` `&` intact while emitting invalid JSON for control bytes ([#355](https://github.com/calvinchengx/fabric-emulator/pull/355)), and the Blob dialect doing the same into XML, where unescaped markup also makes the document malformed ([#356](https://github.com/calvinchengx/fabric-emulator/pull/356)). **The second was found only because the alert stayed open after the first fix** — the search had stopped at the file the alert named instead of the pattern. The alert itself was then dismissed as a false positive with the evidence recorded: its sink is a diagnostics-only `traceWriter` whose embedded `ResponseWriter` hides the Content-Type from the query. | every product and platform repo | family | `contoso-data-product` is the strongest case — it publishes a wheel that every leaf installs, so it is the one repo here whose code reaches users by supply chain rather than by demo. The emulators' workflow is a four-language matrix that ports directly. **Not a claim that anything is wrong in those repos**: it is that nobody has looked, and a clean bill of health from an absent scanner is what this row exists to distinguish |
| G52 | **Squash-merging a stacked PR manufactures phantom conflicts in its child, and they look exactly like real ones.** `fabric-emulator` #377 was squash-merged, so its thirteen commits are not ancestors of `main`. Its child #375 still carried those commits, and git compared them against main's single squashed commit as independent edits to the same lines: **33 conflict hunks across 6 files**, of which 23 classified as genuinely divergent — including a 32-line-vs-13-line hunk in `run.py` where one side had generalised a function the other still hardcoded. **Nothing was actually in disagreement.** `git rebase --onto main <child's base head>` replayed the child's own 10 commits with **zero conflicts**, which is also the proof: real divergence does not rebase clean. **This plan's first reading was wrong and is kept because the error is the point** — the hunks were assessed on their content, judged too semantic to resolve safely, and left alone. The right question was not *what do these hunks mean* but *why do they conflict at all*, and the answer was in the merge strategy, not the code. | any stacked PR in the family; every repo here squash-merges | family | for a stack, **rebase the child onto main rather than merging main into it** — GitHub's *Update branch* button does the merge, which deepens the mess rather than clearing it (it was tried here first). Worth a line in `RULES.md` so the next person reads it before spending an hour on the hunks. The related cost is real: two PRs sat red, and the child also hid a genuine `ty` failure of its own that only surfaced once the phantom conflicts were gone |

## Order of work

Each step is one PR (or one PR per repo it touches), and each names the cell it
turns green and the gap it closes.

| # | step | closes | turns green |
|---|---|---|---|
| ~~0~~ | ~~Commit the verified databricks sources work; publish `contoso-sources`; write this plan~~ **Done.** Vendor-ingest committed (`databricks-platform-jobs` `9355579`); `contoso-sources` published; core **v0.1.1** cut and three consumers pinned | G10 G11 | — |
| ~~1~~ | ~~**Rename and reserve.**~~ **Done.** `contoso-airflow-data-product` → `contoso-data-product-fabric-airflow3` (repo, package name, lockfile, and every reference); the six other leaves created public with LICENSE + README. All 7 leaves and 7 platforms now exist | — | — (shape) |
| 2 | **Unify core.** dbt silver models move down from the Airflow leaf; PySpark path executes the same SQL; silver-metrics equality test; `RULES.md` in core | G1 | — (foundation) |
| ~~3~~ | **Split the platforms that contain their product.** ~~Notebook-pipelines and databricks-jobs~~ **Done — and there was a THIRD.** `snowflake-platform-tasks` held thirteen step modules in `platform/`; they are now `contoso-data-product-snowflake-tasks/steps/`, and the platform takes `PRODUCT` as a path with no Contoso name in its Makefile. The split is what exposed the two couplings that made it worth doing: the internal stage was spelled `<repo>/stages` on BOTH sides and agreed by accident (its failure mode is an empty bronze, not an error), and the workspace credential was reached for rather than handed over. It also emptied the platform's lockfile — no runtime dependency survives, so the thirteen dbt-borne advisories went to the lock that dbt actually runs from. Each leaf pins core by tag | G2 | Fabric · notebooks ✅ honest; Databricks · Jobs stays 🟡; Snowflake · Tasks 🔴 → 🟡 |
| ~~4~~ | ~~**Databricks · Airflow 3.**~~ **Done.** ~~New leaf from the Fabric Airflow 3 leaf~~ — built the other way round, and the plan was wrong about which parent to take: the Fabric Airflow leaf's *shape* is the model (lean binding, cosmos over core's dbt project, no copied SQL) but its *content* is Fabric's, and what this cell needed was the **Databricks steps already witnessed end to end**, re-expressed as a graph. 21/21 task instances, `129,341,157.6700 / 2,800,504.4000 / 474,044`, `PASS=52`, `compare_products` exit 0 five ways — witnessed first on core v0.2.0 and **re-witnessed on v0.3.0** after the leaf's pin moved | — | Databricks · Airflow 3 ✅ |
| ~~4b~~ | ~~**Make the Snowflake emulator answerable.**~~ **Done.** Released v0.1.2 with arm64 and honest failure (G19); typed the result columns so the contracts return verdicts (G18); `LAKEHOUSE_ID` identified as the gold blocker (G14). Ordered before step 5 deliberately — until the contracts could return a verdict, building bronze and silver would have produced numbers nothing could check. That is no longer true, so **step 5 is unblocked**. | G18 G19 | Snowflake · Tasks: 52 contracts return verdicts over an empty silver |
| 5 | **Snowflake · Airflow 3.** ~~SQL bronze, dbt-snowflake silver in core~~ **— that half is DONE and G5 is closed**: bronze lands 580,719 rows by `COPY INTO` from a mounted stage, core's silver project runs unchanged, and the Tasks cell produces the family's money from published artefacts. What is left of this step is the cell itself — a new leaf and platform, both still LICENSE-and-README reservations. ~~Do not start it before **G41**~~ — **that precondition is met**: the steps it would reuse now run `dbt test` and read dbt's own record of what ran, so a second cell would copy a pipeline that can say what it checked | ~~G5~~ ~~G41~~ | Snowflake · Airflow 3 ✅; ~~Snowflake · Tasks moves 🔴 → 🟡~~ **done** |
| 6 | **`dbt_task` in databricks-emulator**, then the Jobs leaf becomes a real Jobs spec | G4 | Databricks · Jobs 🟡 → ✅ |
| 7 | **Airflow-2 leaf** on fabric-emulator's `ApacheAirflowJob`; new platform | G3 | Fabric · built-in Airflow ✅ |
| 8 | **Tasks in snowflake-emulator**, then the Tasks leaf | G6 | Snowflake · Tasks ✅ |
| — | G7 (done, witnessed) and G8/G9 (upstream) run alongside, not in sequence | G7 G8 G9 | contract honesty in every cell |
| — | **Release and pin** whenever core changes — not a step but a rule; see G11 | G11 | — |

Steps 2 and 3 are the ones that make the rest cheap: after them, adding a cell
is a small leaf plus a small platform, and nothing in core moves.

## Definition of done, per cell

A cell is ✅ only when all of these hold. Anything less is 🟡 and says why.

1. The platform clones and runs alone: published wheels, no sibling path, no
   Contoso name. **Standing the source systems up is not a violation** — a
   platform that ingests from four vendors must know how to start them. What
   it must never do is CUSTOMIZE them: sources are as-is, their definitions
   and per-vendor tuning belong to `contoso-sources/sources.yaml`, and a
   platform generates from that declaration rather than restating it. See
   G13, which closed exactly this for `fabric-platform-notebook-pipelines`.
2. The leaf contains only what its platform's team would write, and depends on
   core by **tag** — or by any other immutable published reference where the
   runtime forbids a tag. Fabric's built-in Airflow is Fabric's own image,
   faithfully so because real Fabric ships the same one; it carries no `git`,
   so a leaf running inside it pins a release wheel URL. Same release, same
   immutability, no conflict.
3. `make verify` (or the platform's equivalent) is green **through the
   orchestrator the cell is named for** — not from a host script standing in
   for it.
4. Its `product_snapshot.json` passes `compare_products.py` against the
   family's numbers, and the five contract names match.
5. Every gap it still carries is a row in the table above, not a comment in a
   log.
